controller:
  adminUser: admin
  adminPassword: admin
  serviceType: LoadBalancer
  resources:
    requests:
      cpu: "500m"
      memory: "1Gi"
    limits:
      cpu: "1"
      memory: "2Gi"

persistence:
  enabled: true
  size: 10Gi
  storageClass: gp2
